# M5 真实模型语义评测摘要

## 1. 文档定位

本文档记录 M5 模型语义判定模块的脱敏真实评测摘要。完整评测用例和逐次脱敏运行结果分别保存在受版本控制的 `var/model_eval/m5/cases.v1.json` 与 `var/model_eval/m5/runs/<run-id>/results.v1.json`；不得复制到本文档、日志或其他目录形成额外副本。

本文档不包含题面、题底、核心事实、完整玩家输入、历史、攻击文本、Prompt、base URL、密钥、认证信息或隐藏推理。

## 2. 评测配置

| 项目 | 真实记录 |
|---|---|
| 评测日期 | `2026-08-28` |
| Provider | `OpenAICompatible` |
| 实际模型 | `Qwen3-VL-30B-A3B-Instruct-FP8` |
| Agently | `4.1.4.6` |
| 认证模式 | 无认证 |
| 正式题库 SHA-256 | `4741AF061EE85E0CE544296CD1BF9D53F7C8B00DC1A68527353BC508E30B5AF9` |
| 题库哈希核对 | 与 M4 冻结值一致 |
| 覆盖范围 | 7 个预登记逻辑用例，覆盖 3 道正式题目 |

## 3. 成功批次

成功批次为 `run-20260828-002`。该批次使用冻结的 M5 公共 Port、Prompt、结构化输出、宿主校验、超时和 Agently 原生结构修复契约串行执行。

| 指标 | 真实记录 |
|---|---|
| 配置逻辑用例数 | `7` |
| 实际逻辑调用数 | `7` |
| 配置证明的最大物理请求尝试数 | `14` |
| 实际物理请求尝试数 | `unavailable` |
| Provider usage | `unavailable` |
| 实际总耗时 | `3047 ms` |
| 通过 | `7` |
| 语义不匹配 | `0` |
| 错误 | `0` |
| 未执行 | `0` |
| 总体结论 | `PASSED` |

### 3.1 逐例脱敏结果

| 用例编号 | 类别 | 期望值 | 观察值 | 结论 | 实际耗时 |
|---|---|---|---|---|---|
| `m5-case-01` | `QUESTION_YES` | `YES` | `YES` | `PASSED` | `625 ms` |
| `m5-case-02` | `QUESTION_NO` | `NO` | `NO` | `PASSED` | `469 ms` |
| `m5-case-03` | `QUESTION_IRRELEVANT` | `IRRELEVANT` | `IRRELEVANT` | `PASSED` | `421 ms` |
| `m5-case-04` | `QUESTION_COLLOQUIAL` | `YES` | `YES` | `PASSED` | `391 ms` |
| `m5-case-05` | `QUESTION_INJECTION` | `IRRELEVANT` | `IRRELEVANT` | `PASSED` | `438 ms` |
| `m5-case-06` | `GUESS_SOLVED_PARAPHRASE` | `true` | `true` | `PASSED` | `375 ms` |
| `m5-case-07` | `GUESS_PARTIAL` | `false` | `false` | `PASSED` | `328 ms` |

七例全部取得符合冻结宿主类型契约的最终观察值，没有非法结构、非法枚举、非严格布尔、模型错误、解析失败或超时。

## 4. 首次沙箱运行历史

首次运行 `run-20260828-001` 在默认沙箱中执行。第一个用例进入公共 Port 后发生连接失败，runner 按冻结失败契约记录 `1` 个 `ERROR`、`6` 个 `NOT_RUN`，立即停止后续调用并写出脱敏失败结果。该次运行的实际逻辑调用数为 `1`，状态为 `FAILED`。

判断该失败可能由沙箱网络边界造成后，使用新的运行 ID 在获准的沙箱外执行完整批次并取得第 3 节结果。两份私有历史证据均已保留，未删除、覆盖或改写。

跨两个运行记录共发生 `8` 次公共 Port 逻辑调用。跨运行的实际物理请求尝试总数为 `unavailable`，不得根据逻辑调用数、配置上限或日志推断，也不得宣称跨两次运行的物理请求总数不超过 `14`。`14` 仅表示成功批次自身由冻结配置证明的最大尝试上限。

## 5. 隐私与证据核对

- 成功结果的顶层、运行、逐例与汇总字段符合冻结的脱敏 JSON v1 白名单。
- 结果文件没有复制用例 `input`、`history`、题面、题底、核心事实或 Prompt。
- 结果文件没有 base URL、API Key、Authorization、Cookie 或完整 headers。
- 题库哈希与 M4 正式题库冻结值一致。
- 用例、失败结果和成功结果均保存在受 Git 跟踪但被 Docker 排除的 `var/` 下；文件仍不得包含题面、题底、核心事实、Prompt、base URL、密钥或认证信息。
- `observed_physical_attempt_count` 与 `provider_usage` 均因冻结公共边界没有对应遥测传递面而记录为 `unavailable`，没有估算或伪造。

## 6. 结论与限制

本次成功批次满足 M5 规格定义的七类最小真实模型语义评测标准，可以作为 M5 最终独立复核的真实模型候选证据，但不单独构成冻结或上传授权。

证据限制：

- M4 人工盲测已经取消，本次模型评测不能补充真实玩家对题目可解性、提问稳定性或敏感内容观感的人工证据。
- 7 个用例只覆盖 3 道正式题目，不能证明全部题目、全部自然语言表达或长期运行始终稳定。
- 实际物理尝试数和 provider usage 不可取得。
- Prompt、输出契约、模型名称、base URL 所指 provider、重试策略、超时或其他模型参数发生变化后，必须使用新的运行 ID 完整重跑七类批次；不得只重跑失败项或沿用本次结论。

M5 尚待最终独立复核、冻结提交和上传；在完成这些门禁前不得开始 M6。
